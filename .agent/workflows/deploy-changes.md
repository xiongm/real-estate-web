---
description: Rebuild and restart web and api containers to apply changes
---

1. Rebuild and restart the web and api containers to ensure all changes are applied.
// turbo
2. Run `docker compose up -d --build web api`
