# Release v2.10.8

## Bug Fixes

- **Admin Page Toasts**: Fixed an issue where toast notifications (e.g., "Document uploaded successfully", "Link copied") were appearing twice (one green, one black). Removed duplicate legacy rendering code to ensure only the consistent green toast is shown.
