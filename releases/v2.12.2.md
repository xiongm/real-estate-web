# Release v2.12.2

## Summary
- Configured production build system for the web application (`next build` + `npm start`).
- Migrated security filter logic to a unified server script used in both dev and prod.
- Upgraded web container to `node:22-alpine` to support modern dependencies (`pdfjs-dist`).
- Resolved build errors related to types, imports, and Suspense boundaries.
- Fixed flaky E2E test for checkbox interactions.

## Checks
- [x] API tests
- [x] Web e2e tests
- [x] Manual smoke (verified local production build)

## Notes
- Deployment now uses `npm start` which runs `node scripts/server.js`.
- Docker image build includes `npm run build` step.

