# Release v2.13.2

## Summary
- Fix: Web build now bakes `API_HOST` so `/api` proxy points at the Docker network API instead of `localhost`.
- Fix: `APP_VERSION` is passed at build time so the Admin UI shows the correct version.
- Diagnostics: Added `/api/health` endpoint plus startup logs to surface API target and public base.
- Admin UI: Added a small footer showing current version and API target.
- Ops: Added a cron-friendly healthcheck script (`scripts/healthcheck.py`) that emails on failures using `.env` SMTP settings.

## Checks
- [ ] Manual smoke
